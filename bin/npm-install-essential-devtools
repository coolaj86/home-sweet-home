#!/bin/sh
set -e
set -u

(
    cd /tmp/

    # npm install --location=global \
    #     @socketsecurity/cli@latest

    # socket npm install --location=global \
    #    jswt@2.x \
    npm install --location=global \
        fixjson@1.x \
        jshint@2.x \
        prettier@3.x \
        typescript@5.x \
        typedoc \
        uglify-js@3.x
)
